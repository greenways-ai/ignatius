name: Integrate workspace main policy admission

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by the v2 integration workflow"
